
package com.volvo.java.Maps;

import java.util.Map;
import java.util.Set;

/**
 * Map Properties
 */
public class MapProperties {

    public static void main(String[] args) {
        
  /*    Map: is an Interface ;
         
       1.  A map contains values on the basis of key, i.e. key and value pair. 
          Each key and value pair is known as an entry.
        
       2. A Map doesn't allow duplicate keys, but you can have duplicate value
        
        exception: while trying to add a duplicate key , it replaces the old key
        
       When to use map??    
    V.imp point: A Map is useful if you have to search, update or delete elements on the basis of a key.
        
           
    V.imp point: In Map we can not traversed, so you need to convert it into Set first using keySet() or entrySet() method.
  
   
          HashMap                                          LinkedHashMap                                              TreeMap 
         --------                                          -------------                                              -------  

1. Allows only one null key and                     1. allows only one null key and               1. TreeMap doesn't allow any null key
multiple null values										multiple null values

3. It doesn't maintain any order.                         3. It maintains insertion order                     3. It maintains ascending order.
   (Random order)                                         . 
    
   
   
   Imp Map Methods
   ---------------
   
Set keySet():                                It returns only keys
   
Set entrySet():                              It returns both keys and values

get()                                       dedicated method to print value of a specific key
  
  Decalration of entry set:
  public Set<Map.Entry<K,V>> entrySet()  

==============================================================================================================================    
      
      Map.Entry Interface
      -------------------
      
Entry is the subinterface of Map. So we will access it by Map.Entry name.
It returns a collection-view of the map, whose elements are of this class. 
It provides methods to get "key" and "value".

Methods of Map.Entry interface
----------------------------

K getKey()                                  It is used to obtain a key.
V getValue()                                It is used to obtain value.


        */
    }
}
