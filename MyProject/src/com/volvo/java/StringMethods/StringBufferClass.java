/**
 * 
 */
package com.volvo.java.StringMethods;

/**
 * This class is used to show properties of string buffer class
 */
public class StringBufferClass {

    /* 1. The StringBuffer class in Java is the same as String class except it is mutable 
     * i.e. it can be changed.
     * 
     * 2. Note: Java StringBuffer class is thread-safe i.e. multiple threads cannot access it 
     * simultaneously. So it is safe and will result in an order.
     * 
     * 
     * What is a mutable String?
       A String that can be modified or changed is known as mutable String.
       StringBuffer and StringBuilder classes are used for creating mutable strings.
       
       Important Constructors of StringBuffer Class
      ---------------------------------------------- 
StringBuffer()             =    It creates an empty String buffer with the initial capacity of 16.
StringBuffer(String str)   =    It creates a String buffer with the specified string..
StringBuffer(int capacity) =    It creates an empty String buffer with the specified capacity as length.
     * */
}
